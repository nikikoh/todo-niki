%form{:action => "/boards/#{@board.id}", :method => "POST"}
  %input{:name => "authenticity_token", :type => "hidden", :value => form_authenticity_token}/
  %div
    %label{:for => "board"} Board Name
    %input#board{:name => "board", :required => "", :type => "text"}/
  %div
    %select{:name => "state"}
      - @status.each_with_index do |s,i|
        %option{:value => s}= s
    %label Board State
  %div
    %input{:name => "limit_date", :required => "", :type => "date"}/
    %label Board Limit Day
  %p
    %button{:type => "submit"} create
/ flashメッセージを表示
- flash.each do |msg|
  %p= msg

%table
  %thead
    %tr
      %th State
      %th Limit
      %th Board
      %th
      %th
  %tbody
    - @boards.each do |t|
      %tr
        %td= t.state
        %td= t.limit_date
        %td= t.boards
        %td
          / 次のタスク詳細取得画面へのリンク
          %a{:href => "/boards/#{t.id}"} edit
        %td
          / delete
        %form{:action => "/boards/#{t.id}", :method => "POST"}
          %input{:name => "_method", :type => "hidden", :value => "DELETE"}/
          %input{:name => "authenticity_token", :type => "hidden", :value => form_authenticity_token}/
          　　
          %button{:type => "submit"} delete

