.container
  %p.header_text Boards
  %hr.border_room

  %h1.tabs_text Brackets
  - @tasks.each do |task|
    = render 'commons/task'

.container
  = link_to new_task_path, controller: "tasks", action: "new",id: @task do
    .add_card +Add new task
